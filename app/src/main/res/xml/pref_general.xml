<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:key="line"
        android:title="@string/pref_title_line"
        android:entries="@array/pref_list_line_names"
        android:entryValues="@array/pref_list_line_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="station"
        android:title="@string/pref_title_tube_station"
        android:entries="@array/pref_list_station_names"
        android:entryValues="@array/pref_list_station_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
